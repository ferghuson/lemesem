<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Plateforme d'achat et de vente de Perfect Money">
    <meta name="author" content="Lemesem Exchange">
    <title>{% block title %}{% endblock %} - Lemesem Exchange</title>
    <link href="{{ asset('assets/css/bootstrap.min.css') }}" rel="stylesheet">
    <link href="{{ asset('assets/css/main.css') }}" rel="stylesheet">
    <link href="{{ asset('assets/css/style.css') }}" rel="stylesheet">
    <link rel="icon" type="image/png" href="{{ asset('assets/images/favicon.png') }}"/>
    <link href="https://fonts.googleapis.com/css?family=Merriweather:300,400,700,900|Montserrat:300,400,500,600,700,800" rel="stylesheet">
    <link rel="stylesheet" href="https://site-assets.fontawesome.com/releases/v6.1.1/css/all.css">
    {% block stylesheets %}{% endblock %}
</head>
<body>
<!-- LOADER -->
<div id="loader">
    <div class="position-center-center">
        <div class="ldr"></div>
    </div>
</div>

<main class="container">
    {% if alert is defined %}
        {% if alert  %}
            <div class="alert alert-{{ alert}} alert-dismissible fade show d-flex align-items-center" role="alert">
                {% if alert is same as('danger') %}<i class="fa-light fa-triangle-exclamation me-2"></i>{% endif %}
                {% if alert is same as('success') %}<i class="fa-light fa-circle-check me-2"></i>{% endif %}
                <div> {{ alert_message }} </div>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {% endif %}
    {% endif %}

    {% block content %}{% endblock %}
</main>

<footer class="bg-light position-fixed bottom-0 w-100 text-center">
    <p>My footer</p>
</footer>
<!-- Script -->
<script src="{{ asset('assets/js/jquery-3.6.0.min.js') }}"></script>
<script src="{{ asset('assets/js/bootstrap.bundle.min.js') }}"></script>
<script>
    $(document).ready(function($) {
        $("#loader").delay(500).fadeOut("slow");
        $("#display_pwd a").on('click', function(event) {
            event.preventDefault();
            if($('#display_pwd input').attr("type") == "text"){
                $('#display_pwd input').attr('type', 'password');
                $('#display_pwd i').addClass( "fa-eye-slash" );
                $('#display_pwd i').removeClass( "fa-eye" );
            }else if($('#display_pwd input').attr("type") == "password"){
                $('#display_pwd input').attr('type', 'text');
                $('#display_pwd i').removeClass( "fa-eye-slash" );
                $('#display_pwd i').addClass( "fa-eye" );
            }
        });
    })

</script>
{% block javascripts %}{% endblock %}
</body>
</html>